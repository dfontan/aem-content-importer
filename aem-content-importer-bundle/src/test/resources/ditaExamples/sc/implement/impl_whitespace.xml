<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="concept_FD77AC6D79964AA8913CB50B46A9972C"> 
  <title>Using White Space in Variable Values</title> 
  <shortdesc>In HTML there are several characters that create whitespace. 
  </shortdesc> 
  <prolog> 
	 <metadata> 
		<othermeta name="solution" content="Analytics"/> 
		<othermeta name="topic" content="Developer and implementation"/> 
		<othermeta name="subtopic" content="Troubleshooting"/> </metadata>
  </prolog> 
  <conbody> 
	 <p> These include a space, a tab, and a carriage return (or linefeed).
		Consider the following example: 
		<codeblock outputclass="syntax javascript">&lt;head&gt;
	&lt;title&gt;
		 Home Page
	&lt;/title&gt;
&lt;/head&gt;
&lt;body&gt;
&lt;script language="javascript"&gt;
	s.pageName=document.title
&lt;/script&gt;
</codeblock>
	 </p> 
	 <p>In this case, document.title populates 
		<wintitle>s.pageName</wintitle>, which should receive a value of "Home
		Page." Notice the space before "Home Page." Not all browsers interpret this
		white space in the same way. The result may be either of two examples below: 
		<codeblock outputclass="syntax javascript">s.pageName="Home Page"</codeblock>
		<codeblock outputclass="syntax javascript">s.pageName="	       Home Page"</codeblock>
	 </p> 
	 <p>The first value displays correctly, but the second displays white space
		before the text. 
		<keyword>Analytics</keyword> treats these as distinct values for the 
		<wintitle>s.pageName</wintitle> variable. The 
		<keyword>Analytics</keyword> interface strips the leading white space
		from the second value. The result is a report that displays as shown below. 
	 </p> 
	 <p> 
		<image href="graphics/white_space.jpg"
		 id="image_7293EEA9A9E74A19BD1D8AB53ED81081"> <?xm-replace_text Paragraph?>
		</image> 
	 </p> 
	 <p>This implementation error causes your variable values to be fragmented
		across multiple line items. 
		<keyword>SAINT</keyword> does not allow leading white space in a key
		value. This means that it cannot be used to group multiple line items as a
		work-around if this issue is affecting your site. The only way to fix the
		problem is to pre-process the desired variable value (in this case, the
		document.title property) to remove any leading (or trailing) white space. 
	 </p> 
	 <p>The example above uses the 
		<wintitle>s.pageName</wintitle> variable with the document.title
		property. Adobe does not recommend using document.title as the page name, nor
		does this issue only affect the 
		<wintitle>s.pageName</wintitle> variable. Any variable that has
		leading/trailing white space in its value can be affected. 
	 </p> 
  </conbody> 
</concept>
