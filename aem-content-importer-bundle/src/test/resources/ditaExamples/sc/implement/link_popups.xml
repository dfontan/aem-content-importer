<?xml version="1.0"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd">
<concept id="concept_0AC4BA3A64B84CCB8D9A6021A022D1C3"> 
  <title>Popup Windows with useForcedLinkTracking </title> 
  <shortdesc>When 
	 <codeph>useForcedLinkTracking</codeph> is enabled, AppMeasurement overrides
	 the default link behavior on some browsers to prevent the track link call from
	 being canceled when the new page opens. AppMeasurement executes the track link
	 call and handles the navigation event manually, instead of using the default
	 browser action. 
  </shortdesc> 
  <prolog> 
	 <metadata> 
		<othermeta content="Analytics" name="solution"/> </metadata>
  </prolog> 
  <conbody> 
	 <p>When tracking some links that open a popup window, the Chrome built-in
		popup blocker might prevent AppMeasurement from opening a popup window that
		would normally be allowed. To avoid this, add a 
		<codeph>target="_blank"</codeph> attribute to calls that open a window:
	 </p> 
	 <codeblock outputclass="syntax html">&lt;a href="./popup.html" target="_blank" onClick="&lt;a href="./popup.html" onclick="s.tl(this,'o','popup',null,'navigate');javascript:window.open('./popup.html','popup','width=550,height=450');
return false;"&gt;</codeblock> 
	 <p>This allows the link to be tracked and the popup to load as expected.
	 </p>
  </conbody>
</concept>
