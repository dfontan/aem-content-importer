<?xml version="1.0" encoding="utf-8"?>
<!DOCTYPE concept PUBLIC "-//OASIS//DTD DITA Concept//EN" "concept.dtd"[]>
<concept id="concept_3FC39A3C206A43328B770961C7D898A7">
  <title>JavaScript JS File</title>
  <shortdesc>Verify that the 
	 <filepath>.JS</filepath> file is correctly referenced from the page. The
	 path can be specified either relative to the current document, or an absolute
	 path name can be used.
  </shortdesc>
  <prolog>
    <metadata>
      <othermeta name="solution" content="Analytics" />
      <othermeta name="topic" content="Developer and implementation" />
    </metadata>
  </prolog>
  <conbody>
    <codeblock outputclass="syntax javascript">&lt;script language="JavaScript" 
src="http://www.sampleco.com/javascript/includes/s_code.js"&gt;&lt;/script&gt;</codeblock>
    <p>If some pages of the site are loaded in a secure protocol (https:), and
		reference the 
		AppMeasurement for JavaScript file, ensure that the reference to the file is
		either secure (via https:) or code the reference as shown below. This example
		adopts the protocol of the current page and prevents the warning that "some
		elements are non-secure." 
	 </p>
    <codeblock outputclass="syntax javascript">&lt;script language="JavaScript" 
src="//www.sampleco.com/javascript/includes/s_code.js"&gt;&lt;/script&gt;</codeblock>
    <p>Ensure that the 
		<filepath>.JS</filepath> file on the web servers have permissions
		appropriately set so that the file may be downloaded and executed by website
		visitors. If a different 
		<filepath>.JS</filepath> file is used on development servers, set the
		"read only" attribute for the 
		<filepath>.JS</filepath> file on production servers to avoid an
		overwrite. If altered, ensure that the following settings are set appropriately
		at the top of the 
		<filepath>.JS</filepath> file: 
	 </p>
    <codeblock outputclass="syntax javascript">/************************** CONFIG SECTION **************************/
/* You may add or alter any code config here.                       */
/* Link Tracking Config */
s.trackDownloadLinks=false /* true for download tracking */
s.trackExternalLinks=false /* true for exit link tracking */
s.trackInlineStats=false /* true for ClickMap support */
s.linkDownloadFileTypes="exe,zip,wav,mp3,mov,mpg,avi,doc,pdf,xls"
s.linkInternalFilters="javascript:"
s.linkLeaveQueryString=false
s.linkTrackVars="None" 
s.linkTrackEvents="None"
</codeblock>
    <p>If "<varname>s_account</varname>" is assigned a value at the top of
		the 
		<filepath>.JS</filepath> file, ensure that the report suite ID (populated
		in the 
		<wintitle>s_account 
		</wintitle>variable) is correct. Also ensure that the code in the page is
		not setting the 
		<wintitle>Report Suite ID</wintitle> (<varname>s_account</varname>
		variable). 
	 </p>
    <p>Examine the image request and variables to ensure that the "fallback
		method" (the third part of the "split" code in the example above) is not
		creating the image request instead of the 
		<filepath>.JS</filepath> file. This can be determined since the
		"fallback" method only creates an image request with minimal information. 
	 </p>
  </conbody>
</concept>